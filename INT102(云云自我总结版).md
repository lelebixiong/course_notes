

#时间复杂度
f(n) = O(g(n))
	 算法频度(f(n)): 该算法基本操作需要执行的次数
	 g(n): n取无穷时可近似于f(n)
O(logn) < O(n^1/2) < O(n) < O(nlogn) < O(n^2) < O(2^n)

Binary Search(二分法查找) : 每次查找查两界中间的数
	 如果想找的数比中间的数大, 则把中间+1更新为左界; ...
		  O(logn)

Search Pattern 
	 O(nm)


Selection Sort(选择排序) :  每次从原始数组中选择最小数, append到新的数组中
	 O(n^2)

 Bubble Sort (冒泡排序) : 从前到后, 将原数组的相邻两数进行比较换位
	  第二次只需比较0到n-1个数字 
		   第i次只需比较至n-i+1个数
			    O(n^2)


插入排序 (Insertion Sort Algorithm): 顺序遍历原数组的数, 插入新数组中, 将其放置于小于该数和大于该数的数字之间
	 O(n^2)

#伪代码
#分治法-递归复杂度-归并
#图



#BFS-DFS
1. BFS:  广度优先搜素
	 可解决的问题: 从A出发到B的最短距离问题
	             A是否存在到B的路径

BFS通常使用递归的方法实现
核心思想: 从起始点开始, 逐层访问所有相邻的顶点, 

!!值得注意的是: 对于相同距离的邻接顶点, 访问顺序并不重要,, 因此BFS序列的顺序并不是唯一的

2. DFS: (深度优先搜索) 不撞南墙不回头


#dijkstra(迪杰斯特拉算法)
	 求解最短路径
		  需要dist     final     parent三个部分(final表示访问数组的值(初始为false, 代表还未访问任何顶点), 然后当起始点邻接的顶点均遍历过的时候, 查找dist中最小的(初始dist设为无穷大)&&final要为false, 进行下一轮的for循环)
		  第一步: 跟踪到达每个顶点需要的路程(这里设为dist()), 先将到达各个顶点的距离设置为无穷大
		  第二步: 接下来从dist的数组中找到路程最短的顶点
		  第三步: 通过动态规划不断调整最优路径
		   对于已处理过的点, 不再进行重复访问, 使得其可以在最短的时间内找到最短路径



#二叉树


#贪心算法
求解最小生成树:
		 [[prim算法]] ; [[Kruskal算法]]
		 输入类型: 对每条边都有不同权重的无向图
		 输出数据类型: 最小生成树
			  生成树的权重是树中所有边权重之和


#dp(Dynamic Programming)
	 动态规划

Week6_tutorial

adjacency matrix(邻接矩阵): 每一行和每一列均对应一个顶点
	 矩阵的1表示有边相连; 0表示没有边相连
		    a   b  c  d  e  f  g  h
          a [ 0  1  0  0  1  0  0  0 ]3
          b [ 1  0  1  0  0  0  0  0 ]
          c [ 0  1  0  1  0  1  0  0 ]
          d [ 0  0  1  0  0  0  0  0 ]
          e [ 1  0  0  0  0  0  0  0 ]
          f [ 0  0  1  0  0  0  1  0 ]
          g [ 0  0  0  0  0  0  0  1 ]
          h [ 0  0  0  0  0  0  0  0 ]

adjacency list(邻接表): 以每个顶点为键, 存储与其相邻的顶点列表


#bellmanforlf算法 

(可以求解有负权值 -- 单源最短距的一种算法)

存储方式: 通常采用二维数组, 用第一列来代表起始点, 第二列代表结束点, 实际值代表它的实际权值
更新: 用D来存储距离, 用P来存储上一节点(D上的值与P相加, 小于则更新(只要存在哪怕一次更新, 那么我们就会继续迭代, 直到n - 1次位置(n为顶点的个数)))

#floyd算法
又叫插点法

存储方式: 使用矩阵(通常横坐标为结束点, 纵坐标为出发点)进行迭代

具体流程: 计算Dik + Dij的值并与Dij进行比较, 若更小则Dij的值, 此处i为出发点, k为当前迭代次数对应的点, j为结束点